{
  "name": "carfinance-web",
  "private": true,
  "version": "1.0.0",
  "workspaces": ["ui"],
  "scripts": {
    "dev": "npm run dev -w ui",
    "build": "npm run build -w ui",
    "preview": "npm run preview -w ui"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3"
  }
}
